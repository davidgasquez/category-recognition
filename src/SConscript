paths = ['../include']
flags = ['-Werror', '-g']
libs = ['opencv_core', 'opencv_highgui', 'opencv_imgproc', 'opencv_nonfree', 'opencv_features2d', 'boost_filesystem-mt', 'boost_system-mt']
libpaths = []

#Source files
sources = Glob('*.cc')

Program('../bin/category-reconition', sources, CPPPATH=paths, CPPFLAGS=flags, LIBPATH=libpaths, LIBS=libs)
